# 发言限制功能说明

## 实现时间
2026-05-06

## 功能概述
为普通用户添加发言限制功能，防止刷屏、恶意灌水和垃圾信息，提升聊天室质量。管理员不受任何限制。

## 限制规则

### 1. 发言频率限制
- **最小间隔**: 2秒
- **适用对象**: 普通用户
- **管理员**: 不受限制
- **提示**: "发言过快，请等待X秒后再试"

### 2. 消息长度限制
- **最大长度**: 500字符
- **适用对象**: 所有用户（包括管理员）
- **前端限制**: `maxlength="500"`
- **后端验证**: 超过500字符拒绝
- **提示**: "消息长度不能超过500个字符"

### 3. 重复内容限制
- **检测窗口**: 30秒内
- **检测数量**: 最近5条消息
- **重复阈值**: 相同内容发送≥2次
- **适用对象**: 普通用户
- **管理员**: 不受限制
- **提示**: "请不要重复发送相同的消息"

## 后端实现

### 1. 配置参数

```javascript
const MESSAGE_LIMITS = {
  MIN_INTERVAL: 2000,        // 最小发言间隔（毫秒）- 2秒
  MAX_LENGTH: 500,           // 单条消息最大长度
  DUPLICATE_CHECK_COUNT: 5,  // 检查最近几条消息是否重复
  DUPLICATE_INTERVAL: 30000, // 重复消息检测时间窗口（毫秒）- 30秒
}
```

### 2. 用户发言记录存储

```javascript
// 用户发言记录存储（内存）
const userMessageHistory = new Map()

// 数据结构
{
  "userId": {
    "messages": [
      {
        "text": "消息内容",
        "timestamp": 1746547200000
      }
    ]
  }
}
```

### 3. 自动清理机制

```javascript
// 清理过期的发言记录
setInterval(() => {
  const now = Date.now()
  for (const [userId, history] of userMessageHistory.entries()) {
    // 只保留最近1分钟的记录
    history.messages = history.messages.filter(msg => now - msg.timestamp < 60000)
    if (history.messages.length === 0) {
      userMessageHistory.delete(userId)
    }
  }
}, 60000) // 每分钟清理一次
```

### 4. 检查函数

```javascript
function checkMessageLimit(userId, messageText, isAdmin) {
  // 管理员不受限制
  if (isAdmin) {
    return { allowed: true }
  }

  const now = Date.now()
  
  // 获取用户历史记录
  if (!userMessageHistory.has(userId)) {
    userMessageHistory.set(userId, { messages: [] })
  }
  
  const history = userMessageHistory.get(userId)
  
  // 1. 检查消息长度
  if (messageText && messageText.length > MESSAGE_LIMITS.MAX_LENGTH) {
    return { 
      allowed: false, 
      error: `消息长度不能超过${MESSAGE_LIMITS.MAX_LENGTH}个字符` 
    }
  }
  
  // 2. 检查发言频率
  if (history.messages.length > 0) {
    const lastMessage = history.messages[history.messages.length - 1]
    const timeSinceLastMessage = now - lastMessage.timestamp
    
    if (timeSinceLastMessage < MESSAGE_LIMITS.MIN_INTERVAL) {
      const waitTime = Math.ceil((MESSAGE_LIMITS.MIN_INTERVAL - timeSinceLastMessage) / 1000)
      return { 
        allowed: false, 
        error: `发言过快，请等待${waitTime}秒后再试` 
      }
    }
  }
  
  // 3. 检查重复内容
  if (messageText) {
    const recentMessages = history.messages.filter(
      msg => now - msg.timestamp < MESSAGE_LIMITS.DUPLICATE_INTERVAL
    ).slice(-MESSAGE_LIMITS.DUPLICATE_CHECK_COUNT)
    
    const duplicateCount = recentMessages.filter(
      msg => msg.text === messageText.trim()
    ).length
    
    if (duplicateCount >= 2) {
      return { 
        allowed: false, 
        error: '请不要重复发送相同的消息' 
      }
    }
  }
  
  // 记录本次发言
  history.messages.push({
    text: messageText ? messageText.trim() : '',
    timestamp: now
  })
  
  return { allowed: true }
}
```

### 5. 集成到消息接口

```javascript
app.post('/messages', (req, res) => {
  const newMessage = req.body

  // 验证消息内容
  if ((!newMessage.text || newMessage.text.trim() === '') && 
      (!newMessage.attachments || newMessage.attachments.length === 0)) {
    return res.status(400).json({ error: '消息内容不能为空' })
  }

  // 检查发言限制（管理员不受限制）
  const limitCheck = checkMessageLimit(
    newMessage.userId || newMessage.sender, 
    newMessage.text, 
    newMessage.isAdmin
  )
  
  if (!limitCheck.allowed) {
    return res.status(429).json({ error: limitCheck.error })
  }

  // 继续处理消息...
})
```

## 前端实现

### 1. HTML 结构

**输入框添加最大长度限制**:
```html
<input type="text" id="message-input" 
       placeholder="输入消息... (回车发送)" 
       maxlength="500" />
<button id="send-btn"><i class="fas fa-paper-plane"></i></button>
```

**字符计数器**:
```html
<div class="message-input-info">
  <span id="char-counter" class="char-counter">0/500</span>
</div>
```

### 2. CSS 样式

**发送按钮禁用状态**:
```css
#send-btn:disabled {
  background-color: #ccc;
  cursor: not-allowed;
  transform: none;
}
```

**字符计数器样式**:
```css
.message-input-info {
  padding: 4px 16px;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  font-size: 12px;
  color: var(--text-light);
  min-height: 20px;
}

.char-counter {
  color: var(--text-light);
  transition: color 0.3s;
}

.char-counter.warning {
  color: #ff9800;  /* 橙色警告 */
}

.char-counter.danger {
  color: #f44336;  /* 红色危险 */
  font-weight: 600;
}
```

### 3. JavaScript 逻辑

**字符计数器**:
```javascript
const messageInput = document.getElementById('message-input')
const charCounter = document.getElementById('char-counter')
const MAX_MESSAGE_LENGTH = 500

messageInput.addEventListener('input', () => {
  const length = messageInput.value.length
  charCounter.textContent = `${length}/${MAX_MESSAGE_LENGTH}`
  
  // 根据长度改变颜色
  charCounter.classList.remove('warning', 'danger')
  if (length >= MAX_MESSAGE_LENGTH) {
    charCounter.classList.add('danger')
  } else if (length >= MAX_MESSAGE_LENGTH * 0.8) {
    charCounter.classList.add('warning')
  }
})
```

**发送冷却计时器**:
```javascript
let sendCooldown = false
let cooldownTimer = null

function startSendCooldown(seconds) {
  if (isAdmin) return // 管理员不受限制
  
  sendCooldown = true
  sendBtn.disabled = true
  
  let remaining = seconds
  const originalHTML = sendBtn.innerHTML
  
  const updateButton = () => {
    if (remaining > 0) {
      sendBtn.innerHTML = `<i class="fas fa-clock"></i> ${remaining}s`
      remaining--
      cooldownTimer = setTimeout(updateButton, 1000)
    } else {
      sendBtn.disabled = false
      sendBtn.innerHTML = originalHTML
      sendCooldown = false
    }
  }
  
  updateButton()
}
```

**发送消息前检查**:
```javascript
async function sendMessage() {
  const text = messageInput.value.trim()
  if (!text && attachments.length === 0) return
  
  // 检查是否在冷却中
  if (sendCooldown) {
    toastr.warning('请稍后再发送消息')
    return
  }
  
  // 发送消息...
  
  // 发送成功后启动冷却
  if (!isAdmin) {
    startSendCooldown(2)
  }
}
```

**错误处理**:
```javascript
if (!response.ok) {
  const errorData = await response.json().catch(() => ({}))
  
  if (response.status === 429) {
    // 发言频率限制
    toastr.warning(errorData.error || '发言过快，请稍后再试')
  } else if (response.status === 400) {
    // 其他错误（违禁词、内容为空等）
    toastr.error(errorData.error || '消息发送失败')
  } else {
    toastr.error('消息发送失败')
  }
}
```

## 用户体验

### 1. 字符计数器
```
0-399字符:   灰色显示 "123/500"
400-499字符: 橙色警告 "456/500"
500字符:     红色危险 "500/500" (粗体)
```

### 2. 发送冷却提示
```
发送成功 → 按钮变灰 → 显示倒计时
[⏰ 2s] → [⏰ 1s] → [✈️] 恢复正常
```

### 3. 错误提示
- **发言过快**: 黄色警告提示 "发言过快，请等待2秒后再试"
- **消息过长**: 红色错误提示 "消息长度不能超过500个字符"
- **重复内容**: 红色错误提示 "请不要重复发送相同的消息"

## 限制对比

### 普通用户
| 限制类型 | 规则 | 提示 |
|---------|------|------|
| 发言频率 | 2秒/条 | 发言过快，请等待X秒 |
| 消息长度 | 最多500字符 | 消息长度不能超过500个字符 |
| 重复内容 | 30秒内不能重复2次 | 请不要重复发送相同的消息 |
| 冷却提示 | 发送后2秒冷却 | 按钮倒计时 |

### 管理员
| 限制类型 | 规则 | 说明 |
|---------|------|------|
| 发言频率 | ✅ 无限制 | 可以快速发送 |
| 消息长度 | ⚠️ 500字符 | 前端限制 |
| 重复内容 | ✅ 无限制 | 可以重复发送 |
| 冷却提示 | ✅ 无冷却 | 无倒计时 |

## 工作流程

### 发送消息流程
```
1. 用户输入消息
   ↓
2. 前端检查冷却状态
   ↓
3. 提交到后端
   ↓
4. 后端检查发言限制
   ├─ 频率检查
   ├─ 长度检查
   └─ 重复检查
   ↓
5a. 通过验证 → 保存消息 → 广播 → 启动冷却
5b. 未通过 → 返回错误 → 显示提示
```

### 限制检查顺序
```
1. 消息长度检查
   ↓
2. 发言频率检查
   ↓
3. 重复内容检查
   ↓
4. 违禁词检查
   ↓
5. 保存消息
```

## 安全特性

### 1. 防止刷屏
- ✅ 2秒发言间隔
- ✅ 前端冷却计时器
- ✅ 后端时间戳验证

### 2. 防止灌水
- ✅ 重复内容检测
- ✅ 30秒时间窗口
- ✅ 最近5条消息对比

### 3. 防止长文本攻击
- ✅ 500字符长度限制
- ✅ 前端 maxlength 限制
- ✅ 后端二次验证

### 4. 管理员特权
- ✅ 绕过频率限制
- ✅ 绕过重复检测
- ✅ 快速响应能力

### 5. 内存管理
- ✅ 自动清理过期记录
- ✅ 只保留1分钟历史
- ✅ 定时清理机制

## 配置调整

### 修改发言间隔
```javascript
// 后端 index.js
const MESSAGE_LIMITS = {
  MIN_INTERVAL: 3000,  // 改为3秒
}

// 前端 script.js
if (!isAdmin) {
  startSendCooldown(3)  // 改为3秒
}
```

### 修改消息长度
```javascript
// 后端 index.js
const MESSAGE_LIMITS = {
  MAX_LENGTH: 1000,  // 改为1000字符
}

// 前端 index.html
<input maxlength="1000" />

// 前端 script.js
const MAX_MESSAGE_LENGTH = 1000
```

### 修改重复检测
```javascript
const MESSAGE_LIMITS = {
  DUPLICATE_CHECK_COUNT: 10,  // 检查最近10条
  DUPLICATE_INTERVAL: 60000,  // 1分钟窗口
}
```

## 测试方法

### 1. 频率限制测试
```
1. 发送一条消息
2. 立即再发送一条
3. 应该提示"发言过快，请等待X秒"
4. 等待2秒后
5. 再次发送应该成功
```

### 2. 长度限制测试
```
1. 输入超过500个字符
2. 前端应该自动截断
3. 字符计数器显示红色 "500/500"
4. 尝试发送超长消息
5. 后端应该拒绝
```

### 3. 重复内容测试
```
1. 发送消息 "测试"
2. 等待2秒
3. 再次发送 "测试"
4. 等待2秒
5. 第三次发送 "测试"
6. 应该提示"请不要重复发送相同的消息"
```

### 4. 管理员测试
```
1. 使用管理员账号登录
2. 快速连续发送多条消息
3. 应该没有冷却限制
4. 可以重复发送相同内容
```

### 5. 冷却计时器测试
```
1. 发送一条消息
2. 观察发送按钮
3. 应该显示 [⏰ 2s] → [⏰ 1s]
4. 2秒后恢复正常
5. 按钮可以再次点击
```

## 性能优化

### 1. 内存占用
- 每个用户最多保留1分钟的消息记录
- 自动清理过期数据
- 使用 Map 数据结构提高查询效率

### 2. 时间复杂度
- 频率检查: O(1)
- 长度检查: O(1)
- 重复检查: O(n)，n ≤ 5

### 3. 清理机制
- 每分钟清理一次过期记录
- 避免内存泄漏
- 保持系统性能

## 相关文件

- `index.js` - 后端发言限制逻辑
- `public/index.html` - 输入框和字符计数器
- `public/src/style.css` - 字符计数器样式
- `public/src/script.js` - 前端冷却和计数器逻辑

## 注意事项

### 1. 管理员权限
管理员通过 `isAdmin` 标记识别，不受频率和重复限制。

### 2. 服务器重启
服务器重启后，所有发言记录会丢失。生产环境建议使用 Redis。

### 3. 多服务器部署
当前使用内存存储，多服务器环境下限制可能不一致。建议使用 Redis 共享存储。

### 4. 时区问题
使用 `Date.now()` 获取时间戳，不受时区影响。

### 5. 前端绕过
前端限制可以被绕过，后端验证是最终防线。

## 优化建议

### 1. 使用 Redis 存储
```javascript
const redis = require('redis')
const client = redis.createClient()

// 存储发言记录
await client.zadd(`user:${userId}:messages`, Date.now(), messageText)

// 检查频率
const recent = await client.zrangebyscore(
  `user:${userId}:messages`, 
  Date.now() - 2000, 
  Date.now()
)
```

### 2. 添加 IP 限制
```javascript
// 基于 IP 的频率限制
const ipLimits = new Map()

function checkIPLimit(ip) {
  // 同一IP每秒最多5条消息
}
```

### 3. 动态调整限制
```javascript
// 根据用户等级调整限制
function getMessageLimit(userLevel) {
  if (userLevel >= 10) {
    return { MIN_INTERVAL: 1000 }  // VIP用户1秒
  }
  return { MIN_INTERVAL: 2000 }    // 普通用户2秒
}
```

### 4. 添加统计功能
```javascript
// 记录违规次数
const violationCount = new Map()

function recordViolation(userId) {
  const count = (violationCount.get(userId) || 0) + 1
  violationCount.set(userId, count)
  
  // 超过10次违规，临时禁言
  if (count >= 10) {
    banUser(userId, 3600000) // 禁言1小时
  }
}
```

## 总结

发言限制功能已完整实现：
- ✅ 发言频率限制（2秒间隔）
- ✅ 消息长度限制（500字符）
- ✅ 重复内容限制（30秒内不重复）
- ✅ 字符计数器（实时显示）
- ✅ 发送冷却计时器（倒计时提示）
- ✅ 管理员豁免（不受限制）
- ✅ 友好的错误提示
- ✅ 自动清理机制

有效防止：
- ✅ 刷屏行为
- ✅ 恶意灌水
- ✅ 垃圾信息
- ✅ 长文本攻击

现在聊天室已经有了完善的发言限制保护！
